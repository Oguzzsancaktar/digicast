import React from 'react'
import { CheckCircle } from 'react-feather'
import colors from '../../constants/colors'

const Success = () => {
  return <CheckCircle color={colors.thirth.dark} size={100} />
}

export default Success
