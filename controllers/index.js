const authController = require('./auth-controller/authController')
const registerController = require('./register-controller/registerController')

module.exports = {
  authController,
  registerController
}
